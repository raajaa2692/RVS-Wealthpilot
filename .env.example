# PostgreSQL connection string. Use a managed PostgreSQL provider for production.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/wealthpilot?sslmode=require
# Optional: set false only for a local non-SSL PostgreSQL instance.
DATABASE_SSL=true
# Protect Vercel Cron ingestion endpoint.
CRON_SECRET=replace-with-a-long-random-secret
